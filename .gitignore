# Python
__pycache__/
*.pyc
*.pyo

# Llaves privadas (nunca al repo)
*.pem
*.key
/opt/keys/

# age + sops — clave privada NUNCA al repo; solo va la pública en .sops.yaml
*.age
keys.txt
secrets/*.plain.yaml
secrets/*.dec.yaml

# Rust build artifacts
target/

# IDE — Eclipse, VSCode
.classpath
.project
.settings/
*.swp
*.swo

# Logs
*.log

# Output de diagnósticos — solo conservar los .md (reportes de estado)
output/*
!output/*.md

# opencode (local, no versionar)
.opencode/sessions.db
.opencode/sessions.db-shm
.opencode/sessions.db-wal
.opencode/worktrees/
.opencode/worktrees.db
.opencode/worktrees.log
.opencode/node_modules/
session-ses_*.md
